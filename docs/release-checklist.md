# 上线前检查清单

## 配置

- [ ] `JWT_SECRET` 已替换为强随机值
- [ ] `ANTHROPIC_API_KEY` / 其他上游配置已验证可用
- [ ] `WEB_URL` 与真实管理台域名一致
- [ ] 限流参数（`RATE_LIMIT_PER_MINUTE`、`REDEEM_RATE_LIMIT_PER_MINUTE`）符合运营策略
- [ ] `ADMIN_EMAIL` / `ADMIN_PASSWORD` 已设置并完成首次改密

## 数据库与迁移

- [ ] 生产库已完成备份
- [ ] `prisma migrate deploy` 已在目标环境执行成功
- [ ] `prisma seed` 已完成管理员账号初始化

## 功能验收

- [ ] 用户注册/登录正常
- [ ] 管理员可生成并查询兑换码
- [ ] 用户可兑换并到账
- [ ] `POST /v1/messages` 可成功调用并扣费
- [ ] 管理台可查看用户、用量日志、平台统计

## 监控与日志

- [ ] `GET /health` 可用并接入探活
- [ ] 日志不包含明文 API Key/密码
- [ ] 管理员改权限/改余额可见审计日志

## 回滚预案

- [ ] 已明确镜像回滚版本
- [ ] 已准备数据库回滚/恢复流程
- [ ] 已定义回滚触发条件（错误率、可用性、关键接口失败阈值）
